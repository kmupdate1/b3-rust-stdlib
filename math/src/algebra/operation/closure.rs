/**
 * Closure:
 *
 * An operation is closed over a set when applying
 * the operation to elements of the set produces
 * an element of the same set.
 *
 * Example:
 * Integer + Integer -> Integer
 */

pub trait ClosureOperation<Params, Result> {}
